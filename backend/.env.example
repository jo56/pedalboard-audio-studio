# CORS Configuration
CORS_ALLOWED_ORIGINS=https://your-frontend.pages.dev

# Rate Limiting (set to true for local development only)
LOCAL_DEPLOYMENT=false

# Debug Mode (never enable in production)
# DEBUG=false
